
class Solution {
    int isPalindrome(String S) {
        int n=S.length();
       for(int i=0;i<S.length()-1;i++)
       {
           if(S.charAt(i)!=S.charAt(n-i-1))
           {
               return 0;
           }
       }
       return 1;
        // code here
    }
};
