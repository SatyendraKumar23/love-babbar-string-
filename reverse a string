class Solution {
    public void reverseString(char[] s) {

        int n=s.length-1;
        int m=0;

        while(m<=n)
        {
            int  temp=s[n];
            s[n]=s[m];
            s[m]=(char)temp;

            m++;
            n--;
            
        }
        
    }
